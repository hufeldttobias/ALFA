# Copy to .env for local runs. Do not commit real secrets.
#
# Railway: add the Postgres plugin to your service — DATABASE_URL is injected automatically.
# For running Flask locally against Railway Postgres, paste DATABASE_PUBLIC_URL from the DB service.

DATABASE_URL=
# DATABASE_PUBLIC_URL=
